# Claude Headless Job Runner Example

```bash
claude -p "Run hive job run --workspace $HIVE_WORKSPACE --json and explain failures only."
```

Claude wrappers must call the canonical Hive CLI/MCP operations.
